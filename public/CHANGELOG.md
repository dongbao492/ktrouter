# Changelog

No public changelog is configured for this build.

Set `GITHUB_CONFIG.changelogUrl` in `src/shared/constants/config.js` if you want to load changelog content from a repository or another static file.
